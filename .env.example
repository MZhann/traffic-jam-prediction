# Reference template. Each workspace has its own .env.example with the
# variables it actually reads — copy those to web/.env.local, api/.env, ml/.env.

# api/
MONGODB_URI=mongodb://localhost:27017/trafficjam
JWT_SECRET=change-me-in-prod
OPENWEATHER_API_KEY=
ML_SERVICE_URL=http://localhost:8000

# web/
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_MAPGL_KEY=

# ml/
MODEL_VERSION=mvp-rule-based-v1
